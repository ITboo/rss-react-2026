export enum MESSAGES {
  LOADING = 'Loading...',
  NOT_FOUND = "Ooops. Nothing's here",
  ERROR = "Something's went wrong",
}

export enum UI {
  LOCATION = 'Location',
  SEARCH = 'Search',
  REFRESH = 'Refresh the page'
}
